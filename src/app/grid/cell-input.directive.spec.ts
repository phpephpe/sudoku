import { CellInputDirective } from "./cell-input.directive";

describe("MinDirective", () => {
  it("should create an instance", () => {
    const directive = new CellInputDirective();
    expect(directive).toBeTruthy();
  });
});
